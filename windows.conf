##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hypr.land/Configuring/Window-Rules/ for more
# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

# Example windowrules that are useful

windowrule {
    # Ignore maximize requests from all apps. You'll probably like this.
    name = suppress-maximize-events
    match:class = .*

    suppress_event = maximize
}

windowrule {
    # Fix some dragging issues with XWayland
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}

# Hyprland-run windowrule
windowrule {
    name = move-hyprland-run

    match:class = hyprland-run

    move = 20 monitor_h-120
    float = yes
}
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# workspace = 1, persistent: 1, defaultName: Console,
# workspace = 2, persistent: 1, defaultName: Files,
# workspace = 3, persistent: 1, defaultName: Browser,
# workspace = 4, persistent: 1, defaultName: Messengers,
# workspace = 5, persistent: 1, defaultName: Godot,
# workspace = 6, persistent: 1, defaultName: GameSpace,
# workspace = 7, persistent: 1, defaultName: Media,
# workspace = 8, persistent: 1, defaultName: GameLaunchers,

# workspace = w[t1], 

workspace = 1, monitor:HDMI-A-1
workspace = 4, monitor:DVI-D-1
workspace=9,monitor:HDMI-A-1
windowrulev2 = workspace 9, class:^(steam_app_.*)$
windowrulev2 = fullscreen, class:^(steam_app_.*)$
# windowrule = opacity 1.0 override 1.0 override 0.8, workspace: -98
# windowrule = noblur, workspace: -98

# windowrule = opacity 1.0 override 1.0 override, class:(com\.obsproject\.Studio)
windowrule = match:class (com\.obsproject\.Studio), workspace special:magic silent
windowrule = match:class (soundux), workspace special:magic
windowrule = match:class (pulseshitter), workspace special:magic, float true, size 706 644, move monitor_w-716 50
windowrule = match:class (brave-browser), workspace 3
windowrule = match:title (.*is sharing your screen.*), workspace 3 silent, move -100 -100
windowrule = match:class (.*telegram.*), workspace 4
windowrule = match:class (.*telegram.*), match:title (Choose Files), size 860 720, center true
windowrule = match:class (.*telegram.*), match:title (TelegramDesktop), size 860 720, center true
windowrule = match:class (.*discord.*), workspace 4
windowrule = match:class (.*vesktop.*), workspace 4
windowrule = match:class (Godot), workspace 5
windowrule = match:class (.*godotengine.*), workspace 5, tile true
windowrule = match:title (notificationtoasts.*), workspace 6 silent, no_initial_focus true, pin true
windowrule = match:class (Spotify), workspace 7
windowrule = match:class (brave-.*-Default), match:title (YouTube.*), workspace 7
windowrule = match:title (SKlauncher.*), workspace 8
windowrule = match:title (r2modman), workspace 8
windowrule = match:class (heroic), workspace 8
windowrule = match:class (.*lutris.*), workspace 8
# windowrule = match:class (steam), workspace 8 silent
windowrule = match:title (Sign in to Steam), workspace 8 silent
windowrule = match:title (Steam), workspace 8
windowrule = match:title (Special Offers), workspace 8
windowrule = match:title (Friends List), workspace 8
windowrule = match:title (Shutdown), workspace 8 silent

# windowrule = tile, class:(.*godotengine.*)
# windowrule = tile, title:(.*Godot.*)
# windowrule = move 1573 52, class:(Godot), title:(Quick Settings)
# windowrule = size 764 1016, class:(Godot), title:(Create New Node)
# windowrule = move 12 52, class:(Godot), title:(Create New Node)
# windowrule = center 1, class:(Godot), title:(Open a .*)
# windowrule = size 900 700, class:(Godot), title:(Open a .*)
# windowrule = center 1, class:(Godot), title:(Import Existing Project)
# windowrule = size 900 700, class:(Godot), title:(Import Existing Project)
# windowrule = stay_focused, class:(Godot), title:(Please Confirm...)

windowrule = match:class (T|t)(hunar), match:title (Rename.*), stay_focused true, float true
windowrule = match:class (imv), float true, size 1280 720, center true
windowrule = match:class (mpv), float true, size 1280 720, center true
windowrule = match:class (engrampa), float true
windowrule = match:title (Open Files), float true, dim_around true
windowrule = match:title (.*wants to save), float true, dim_around true
windowrule = match:title (Create New Folder), stay_focused true
windowrule = match:title (.*Picture in picture), pin true, float true, size 960 540
windowrule = match:class (polkit-gnome-authentication-agent-1), pin true, dim_around true, stay_focused true
windowrule = match:title (Mission Center), center true, float true, size 1496 786
windowrule = match:class (hyprland-share-picker), float true, dim_around true, size 500 290
windowrule = match:title (.*File Operation Progress), float true, no_initial_focus true, size 500 110, move monitor_w-510 50
windowrule = match:class (clipse), pin true, stay_focused true, float true, size 546 621, move monitor_w-556 50
windowrule = match:class (.*pavucontrol), pin true, float true, size 630 616, move monitor_w-640 50
windowrule = match:class (blueman-manager), size 630 616, move monitor_w-640 50
windowrule = match:class (gsimplecal), pin true, float true, move monitor_w-240 50
windowrule = match:class (.*nm-connection-editor), pin true, float true, size 630 616, move monitor_w-640 50
windowrule = match:class (Tk), float true, size 1280 720

# screenshot fix
# windowrule = stay_focused, title:(glxgears)
# windowrule = size 0 0, title:(glxgears)
# windowrule = move -99 -99, title:(glxgears)
# windowrule = workspace special:magic silent, title:(glxgears)

# xwaylandvideobridge working configuration
# windowrule = opacity 0.0 override 0.0 override 0.0 override,class:^(xwaylandvideobridge)$
# windowrule = noanim,class:^(xwaylandvideobridge)$
# windowrule = no_initial_focus,class:^(xwaylandvideobridge)$
# windowrule = size 0 0,class:^(xwaylandvideobridge)$
# windowrule = move -99 -99,class:^(xwaylandvideobridge)$
# windowrule = noblur,class:^(xwaylandvideobridge)$
